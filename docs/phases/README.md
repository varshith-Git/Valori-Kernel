# Phase Reports

One report per delivered phase of the multi-node roadmap
([docs/MULTINODE_ROADMAP.md](../MULTINODE_ROADMAP.md)). Each report records
what shipped, what was found along the way, and the validation evidence —
so the history of *why* the codebase looks the way it does survives the
people and sessions that built it.

## Status

| Phase | Report | Commit | Status |
|---|---|---|---|
| P8 — CI hardening: `coverage` job (llvm-cov on valori-kernel, lcov artifact + job-page summary) + `miri` job (nightly; `tests/fxp.rs` + `tests/proof.rs`); `rust-setup` composite action gains `toolchain` + `components` inputs; full CI job map documented | [phase-P8-ci-hardening.md](phase-P8-ci-hardening.md) | `Node-scaleup` | ✅ done |
| P6 — InsertReceipt: `InsertReceipt { record_id, old_root, new_root, proof, sequence, timestamp, state_hash }` in `valori-kernel/src/proof.rs`; `POST /v1/records` returns receipt on both standalone and cluster paths; `state_hash` = BLAKE3 self-hash (tamper-evident seal); `insert_with_receipt()` in Python SDK (sync + async); 5 new kernel tests; 153 kernel tests (was 148) | [phase-P6-insert-receipt.md](phase-P6-insert-receipt.md) | `Node-scaleup` | ✅ done |
| K4 — Snapshot version migration tests: hand-rolled V1–V6 legacy encoders, 10 tests (`v1_decodes_correctly`..`v6_decodes_correctly`, hole-slot, cross-version hash-stable reencode chain, two decoder-hardening tests); mutation-tested to confirm sensitivity; 148 kernel tests (was 138) | [phase-K4-snapshot-version-migration.md](phase-K4-snapshot-version-migration.md) | `Node-scaleup` | ✅ done |
| M5–M6 — Package Store + Integrity Manager: `PackageStore` (atomic install, `InstallLock`, per-package `manifest.json`), `IntegrityManager` (verify/verify_all/repair), `GarbageCollector` + `RefCounter`, `SystemHealth`; `GET /v1/models/health` on both routers; 75 model tests (was 42) | [phase-M5-M6-package-store.md](phase-M5-M6-package-store.md) | `Node-scaleup` | ✅ done |
| M1–M4 — `valori-models` package manager: `ModelManifest` (15-field typed manifest replacing `InstalledModel`+`ModelSpec`), `ProviderRegistry` (no match statements), `Resolver` (task+dim based selection), M4 download state machine (`DownloadJob` + `DownloadState` + progress channel); 42 model tests | [phase-M1-M4-model-manager.md](phase-M1-M4-model-manager.md) | `Node-scaleup` | ✅ done |
| E4 — Ingest observability: `CancellationToken`, `RetryPolicy`, `PipelineConfig` (batch streaming), `PipelineHook`, `ProgressEvent` channel, `StageMetrics`/`StageResult`/`PipelineResult`; `run_observed()` on `IngestPipeline`; `run()` stays backward-compatible; 110 ingest tests (was 89) | [phase-E4-observability.md](phase-E4-observability.md) | `Node-scaleup` | ✅ done |
| E3.1–E3.6 — Extractor framework: `Extractor` trait + 5 impls, `ExtractorRegistry` (extension/MIME/bytes dispatch + `infer` magic-byte detection), `DocumentMetadata` (typed), `DocumentValidator`, `DocumentSource` enum, `ReaderCapabilities`; 89 ingest tests (was 49) | [phase-E3.1-E3.6-extractor-framework.md](phase-E3.1-E3.6-extractor-framework.md) | `Node-scaleup` | ✅ done |
| E3.5 — `ReaderRegistry`: `reader_for_extension(ext)` + `reader_for_path(path)` → `Arc<dyn Reader>`; all format dispatch in one place; 49 ingest tests (was 34) | [phase-E3.5-reader-registry.md](phase-E3.5-reader-registry.md) | `Node-scaleup` | ✅ done |
| E3 — Format readers: `MarkdownReader` (pulldown-cmark), `HtmlReader` (scraper, script/style pruned), `PdfReader` (pdf-extract + lopdf, spawn_blocking), `DocxReader` (zip + quick-xml, w:t runs + core props); 34 ingest tests (was 19) | [phase-E3-format-readers.md](phase-E3-format-readers.md) | `Node-scaleup` | ✅ done |
| E2.5 — KernelWriter wired: `ingest()` handler replaced with `IngestPipeline::run()` + `KernelWriter`; ~200 lines of inline orchestration deleted; sync and async paths both use the pipeline; 263 node tests, 19 ingest tests | [phase-E2.5-kernel-writer.md](phase-E2.5-kernel-writer.md) | `Node-scaleup` | ✅ done |
| E2 — Composable ingest pipeline: `Document` + `Reader`/`Chunker`/`Embedder`/`Writer` traits + `IngestPipeline`; `TextReader`, `ValoriChunker`, `ModelProviderEmbedder` (calls `Box<dyn ModelProvider>`), `NoopWriter`; existing `embed.rs`/`handler.rs` untouched; 18 tests | [phase-E2-composable-ingest.md](phase-E2-composable-ingest.md) | `Node-scaleup` | ✅ done |
| E1.1 — `valori-models` standalone crate: `ModelProvider` trait (`embed`/`health`/`kind`/`dim`), `build()` factory (Ollama/OpenAI/Voyage/Dummy), `ModelStore` DIP seam, `JsonModelStore`, streaming SHA-256 download, `VerifyStatus`, built-in registry (11 models), workspace deps fixed; 5 tests | [phase-E1.1-valori-models-crate.md](phase-E1.1-valori-models-crate.md) | `Node-scaleup` | ✅ done |
| E1 (lite) — Model Manager: daemon model catalog — `ModelStore` seam + `JsonModelStore`, curated registry (OpenAI/Ollama/BGE-ONNX), `GET /v1/models` (installed+available+disk), install (remote=register, local=SHA-256-verified streaming download), `GET\|DELETE /v1/models/*id`; management only (no inference); 17 tests | [phase-E1-model-manager.md](phase-E1-model-manager.md) | `Node-scaleup` | ✅ done |
| D2.2 — Restart loop & health FSM: operational `Supervisor` (policy + capped exponential backoff + crash tracking) restarts crashed nodes; `Runtime::poll_exits` crash detection; `RuntimeState` gains `Recovering`; background monitor task; `restart_policy` per project + `supervision` in API; e2e kills a real node and asserts restart; 14 tests | [phase-D2.2-restart-loop.md](phase-D2.2-restart-loop.md) | `Node-scaleup` | ✅ done |
| D1.3 — Installers + clean-machine validation groundwork: fixed a real pre-existing Next.js 14→16 params bug that blocked `next build` outright; `ui/` bundled as a Node sidecar (`prepare-ui-server.mjs`, `ui_server_manager.rs`, fixed port `17862`, loading-page → `navigate()` handoff); first real `tauri build` in this project (`.dmg` produced + `hdiutil verify`-clean); found + fixed a real SIGTERM orphan bug AND a latent `AppHandle::exit()` re-entrancy risk in the shutdown path via actual launch testing; `.github/workflows/desktop-build.yml` (macOS/Windows/Linux matrix); `docs/architecture/desktop-layout.md` + `docs/DESKTOP_RELEASE_CHECKLIST.md` (new) | [phase-D1.3-installers.md](phase-D1.3-installers.md) | `Node-scaleup` | ✅ done |
| D3.1 — Sidecar bundling: `valori-daemon` + `valori-node` bundled into the desktop app via Tauri `externalBin`; `desktop/scripts/{prepare-sidecars,dev}.mjs` (build/dev-time binary prep, host-triple resolution); `daemon_manager.rs` gains exactly two code paths (dev target-dir search vs. release sidecar spawn via `tauri-plugin-shell`), a version handshake (`GET /version` api-level check → `UnsupportedVersion` on mismatch), and `VALORI_NODE_BIN` wiring so the daemon can find its bundled node sidecar with no Cargo/target-dir assumption | [phase-D3.1-sidecar-bundling.md](phase-D3.1-sidecar-bundling.md) | `Node-scaleup` | ✅ done |
| D3 — Desktop launches and manages the daemon: `POST /v1/shutdown` (graceful, portable cross-platform shutdown mechanism); `Runtime::stop_all()` fixed to snapshot-then-terminate (was hard-killing nodes with no snapshot); `desktop/src-tauri/src/daemon_manager.rs` (`start_daemon`/`stop_daemon`/`daemon_status` Tauri commands, spawns real `valori-daemon` with `VALORI_HOME` from the chosen workspace, on-exit graceful-shutdown hook); Welcome wizard's workspace folder now actually becomes `VALORI_HOME` | [phase-D3-desktop-launches-daemon.md](phase-D3-desktop-launches-daemon.md) | `Node-scaleup` | ✅ done |
| D2.1 — Dependency-inversion seams: `ProjectStore`/`WorkspaceStore`/`EventStore`/`Launcher` traits (JSON/Memory/Local impls today; Docker/SQLite later, no daemon change); `RuntimeState` state machine (illegal transitions error); `RestartPolicy` moved to operational layer; `Daemon` fully injected via `DaemonDeps`; 11 tests | [phase-D2.1-inversion-seams.md](phase-D2.1-inversion-seams.md) | `Node-scaleup` | ✅ done |
| D2 — Node Runtime: decomposed `Supervisor` into a `Runtime` trait + `LocalRuntime` (+ `PortAllocator`, `ResourceMonitor`, `RestartPolicy`); `Box<dyn Runtime>` so Docker/SSH/remote slot in with no daemon change; `EventLog` + `GET /v1/events`; per-node `GET .../runtime` (CPU/RAM via ps); stable UUID ids on projects/workspaces; 10 tests | [phase-D2-node-runtime.md](phase-D2-node-runtime.md) | `Node-scaleup` | ✅ done |
| D1.1 — Stabilize the Daemon API: system/discovery (`/v1/system`, `/version`, `/v1/config`), Workspaces CRUD (real, `default` always exists), Collections proxied to the running node, node logs + uptime, Models stubs (501); all versioned under `/v1`; 7 tests | [phase-D1.1-daemon-api.md](phase-D1.1-daemon-api.md) | `Node-scaleup` | ✅ done |
| D1 — Valori Daemon, Milestone 1: new `valori-daemon` crate + binary; project lifecycle (create/delete/start/stop/restart/list) + process supervision over HTTP; Rust port of `process-manager.ts`; supervises real `valori-node` children with internal port allocation; 5 tests incl. real spawn e2e | [phase-D1-daemon-milestone1.md](phase-D1-daemon-milestone1.md) | `Node-scaleup` | ✅ done |
| N5 — valori-engine extraction: `Engine` struct (1 743-line engine.rs) → new `valori-engine` crate; `EngineFromNodeConfig` extension trait bridges `NodeConfig` → `EngineConfig`; DIP: vault + object_store injected by caller; 5 crate tests; 31 test files + main + examples updated | [phase-N5-valori-engine-extraction.md](phase-N5-valori-engine-extraction.md) | `Node-scaleup` | ✅ done |
| N4 — valori-ingest extraction: `embedder.rs` + chunking logic → new `valori-ingest` crate; `ingest_document` handler is stateless and compiles into both routers; `embed_config_from_node` helper in engine.rs; recursion bug in tree→auto fallback fixed; 12 crate tests | [phase-N4-valori-ingest-extraction.md](phase-N4-valori-ingest-extraction.md) | `Node-scaleup` | ✅ done |
| N3 — valori-rag extraction: `graph_rag.rs` + `tree_rag.rs` + `community.rs` → new `valori-rag` crate; `LlmConfig` introduced to break EmbedConfig coupling; 13 crate tests; both routers + capabilities + engine migrated | [phase-N3-valori-rag-extraction.md](phase-N3-valori-rag-extraction.md) | `Node-scaleup` | ✅ done |
| N2 — valori-index extraction: `structure/{hnsw,ivf,bq,index,quant,deterministic}` → new `valori-index` crate; single `VectorIndex` trait; engine + 5 integration test files migrated; structure/ deleted; 21 crate tests | [phase-N2-valori-index-extraction.md](phase-N2-valori-index-extraction.md) | `Node-scaleup` | ✅ done |
| N1 — valori-search extraction: `decay.rs` + `valori_reranker.rs` → new `valori-search` crate; `matches_metadata_filter` extracted from `api.rs`; O(1) IDF via inverted index; `restore_corpus` made deterministic; 27 tests | [phase-N1-valori-search-extraction.md](phase-N1-valori-search-extraction.md) | `Node-scaleup` | ✅ done |
| P4 — Persistence contract corpus: committed V7 snapshot fixtures + WAL fixtures + event-log fixtures with TOML manifests pinning event_count/record_count/chain_head/state_hash; 3 malformed-artifact hardening tests; dual-path verify (recover_from_event_log + valori_verify::verify_log_file) | [phase-P4-persistence-contracts.md](phase-P4-persistence-contracts.md) | `Node-scaleup` | ✅ done |
| K3b — Coverage tests: 43 new tests filling zero-coverage modules (`fxp/ops.rs`, `proof.rs`, `verify.rs`, `adapters/ivecs.rs`); dead binary-protocol types deleted from `types/mod.rs`; 134 kernel tests total | [phase-K3b-coverage-tests.md](phase-K3b-coverage-tests.md) | `Node-scaleup` | ✅ done |
| K3 — Coverage audit: `cargo-tarpaulin` baseline; 36.24% kernel coverage; zero-coverage modules ranked by risk: `hnsw.rs` (265L), `proof.rs` (24L), `fxp/ops.rs` (21L), `verify.rs` (4L), `types/mod.rs` (48L), `adapters/ivecs.rs` (11L) | [phase-K3-coverage-audit.md](phase-K3-coverage-audit.md) | `Node-scaleup` | ✅ done |
| K2 — Command removal: deleted `Command` enum from kernel + `ValoriKernel` struct + CRC64 `state_hash`; WAL upgraded to v2 format (writes `KernelEvent + ns` directly); `LegacyWalCommand` in storage for v1 backward-compat reading only; deleted 3 legacy bench bins | [phase-K2-command-removal.md](phase-K2-command-removal.md) | `Node-scaleup` | ✅ done |
| K1 — Replay unification: inlined `apply(Command)` into `apply_event_ns(KernelEvent)`; deleted `replay.rs` + `WalHeader`; fixed version-bump omissions for 4 event types; `apply_raw_for_test` → `apply_event_for_test(KernelEvent)` | [phase-K1-replay-unification.md](phase-K1-replay-unification.md) | `Node-scaleup` | ✅ done |
| E5 — HNSW namespace wiring: wired HNSW/IVF/BQ into `search_l2_ns`; fixed sort-order bug in `search_layer` (inverted graph) and over_fetch bug (k×20 → k); confirmed O(log N) in-process at N=50k, ~183× speedup vs brute | [phase-E5-hnsw-namespace-wiring.md](phase-E5-hnsw-namespace-wiring.md) | `Node-scaleup` | ✅ done |
| E4 — ExecutionResources: `tree_cache` + `community_store` extracted from `Engine` into `ExecutionResources` struct; application-layer boundary now explicit | [phase-E4-execution-resources.md](phase-E4-execution-resources.md) | `Node-scaleup` | ✅ done |
| E3 — Hide pub state: `Engine.state` changed to `pub(crate)`; 8 public accessor methods added (`record_count`, `node_count`, `edge_count`, `kernel_dim`, `get_node`, `outgoing_edges`, `get_record`, `get_edge`, `kernel_state`, `clone_kernel_state`); FFI stale dual-branch patterns removed | [phase-E3-hide-pub-state.md](phase-E3-hide-pub-state.md) | `Node-scaleup` | ✅ done |
| E2 — NamespaceRegistry → CollectionRegistry: dead `NamespaceRegistry` struct in engine.rs deleted; `valori-metadata::CollectionRegistry` is now the single implementation; `list()` added to CollectionRegistry | [phase-E2-namespace-registry.md](phase-E2-namespace-registry.md) | `Node-scaleup` | ✅ done |
| E1 — Single persistence funnel: Engine's dual `Option<EventCommitter>`/`Option<WalWriter>` branches (duplicated across 10+ write methods) collapsed into `Persistence` enum + one `commit_and_apply_ns` write path; fixed missing auto-tier check on event-log batch inserts | [phase-E1-persistence-funnel.md](phase-E1-persistence-funnel.md) | `Node-scaleup` | ✅ done |
| E0 — Dead-file cleanup: deleted 10 stale storage-layer duplicates left in valori-node by the Phase 1.1 restructure (recovery.rs had already drifted); new `tests/architecture.rs` tripwire fails on any cross-crate source-file duplication | [phase-E0-dead-file-cleanup.md](phase-E0-dead-file-cleanup.md) | `Node-scaleup` | ✅ done |
| R5 — Async ingestion pipeline & route parity: background task-driven ingestion via `POST /v1/ingest?async=true` + `GET /v1/ingest/status/:job_id`; fixed critical state sync bug in `Engine::create_edge` and wire format v4 verification parity | [phase-R5-async-ingestion.md](phase-R5-async-ingestion.md) | `Node-scaleup` | ✅ done |
| R3 — Dual-path unification: memory domain (upsert, search, consolidate, contradict) migrated to shared `routes/` handler pattern; eradicated the "Two Kitchens" problem | [phase-R3-dual-path-memory.md](phase-R3-dual-path-memory.md) | `Node-scaleup` | ✅ done |
| R2 — Dual-path unification: graph/records/meta/version domains migrated to `routes/`; fixed cluster list-nodes tenant leak, kind-coercion, meta wire divergence; new `POST /v1/soft-delete` (standalone) + `DELETE /v1/graph/node/:id` (cluster); METHOD_GAPS now empty | [phase-R2-dual-path-domains.md](phase-R2-dual-path-domains.md) | `Node-scaleup` | ✅ done |
| R1 — Dual-path unification: shared `routes/` handler pattern (collections migrated first), route-parity test guarding all `/v1` endpoints across both routers; fixed 400/404 drop divergence + missing cluster name validation | [phase-R1-dual-path-unification.md](phase-R1-dual-path-unification.md) | `Node-scaleup` | ✅ done |
| 0 — Baseline durability & verifier | [phase-0-baseline.md](phase-0-baseline.md) | merged via PR #3 (`57da43e`) | ✅ done |
| 1.1 — Workspace restructure | [phase-1.1-workspace-restructure.md](phase-1.1-workspace-restructure.md) | `2bd793d` | ✅ done |
| 1.1b — Per-crate test layout + kernel fixes | [phase-1.1b-per-crate-tests.md](phase-1.1b-per-crate-tests.md) | `1db62c9` | ✅ done |
| 1.2 — valori-wire + segment format v3 | [phase-1.2-valori-wire-v3.md](phase-1.2-valori-wire-v3.md) | `b4ac53b` | ✅ done |
| 1.3 — FxpFormat seam (configurable precision) | [phase-1.3-fxpformat-seam.md](phase-1.3-fxpformat-seam.md) | `22f600b` | ✅ done |
| 1.4 — Collections seam | [phase-1.4-collections-seam.md](phase-1.4-collections-seam.md) | `41fe5b6` | ✅ done |
| 1.5 — Crypto-shredding design (GDPR) | [phase-1.5-crypto-shredding.md](phase-1.5-crypto-shredding.md) | `003ce7e` | ✅ done |
| 1.6 — Security design doc | [phase-1.6-security-model.md](phase-1.6-security-model.md) | see git log | ✅ done |
| 1.7 — Verifier hardening (limits + fuzzing) | [phase-1.7-verifier-hardening.md](phase-1.7-verifier-hardening.md) | see git log | ✅ done |
| 1.8 — Storage policy (snapshot cadence, zstd, disk-full) | [phase-1.8-storage-policy.md](phase-1.8-storage-policy.md) | see git log | ✅ done |
| 1.9 — Committer trait seam | [phase-1.9-committer-trait.md](phase-1.9-committer-trait.md) | see git log | ✅ done |
| 1.10 — CI upgrades (multi-arch hash equality, cargo-deny) | [phase-1.10-ci-upgrades.md](phase-1.10-ci-upgrades.md) | see git log | ✅ done |
| 1.11 — Docker + compose | [phase-1.11-docker-compose.md](phase-1.11-docker-compose.md) | see git log | ✅ done |
| 2.1 — openraft type config | [phase-2.1-openraft-types.md](phase-2.1-openraft-types.md) | see git log | ✅ done |
| 2.2 — Raft log store | [phase-2.2-raft-log-store.md](phase-2.2-raft-log-store.md) | see git log | ✅ done |
| 2.3 — Raft state machine (kernel + audit) | [phase-2.3-raft-state-machine.md](phase-2.3-raft-state-machine.md) | see git log | ✅ done |
| 2.4 — gRPC transport (tonic) | [phase-2.4-grpc-transport.md](phase-2.4-grpc-transport.md) | see git log | ✅ done |
| 2.5 — RaftCommitter + cluster bootstrap | [phase-2.5-raft-committer.md](phase-2.5-raft-committer.md) | see git log | ✅ done |
| 2.6 — Cluster management API | [phase-2.6-cluster-api.md](phase-2.6-cluster-api.md) | see git log | ✅ done |
| 2.7 — Snapshot transfer | [phase-2.7-snapshot-transfer.md](phase-2.7-snapshot-transfer.md) | see git log | ✅ done |
| 2.8 — Fault-tolerance tests | [phase-2.8-fault-tolerance.md](phase-2.8-fault-tolerance.md) | see git log | ✅ done |
| 2.9 — Admin audit events in chain | [phase-2.9-admin-audit-events.md](phase-2.9-admin-audit-events.md) | see git log | ✅ done |
| 2.10a — Persistent Raft log (redb) | [phase-2.10a-persistent-raft-log.md](phase-2.10a-persistent-raft-log.md) | see git log | ✅ done |
| 2.10b — mTLS (rustls + cluster CA) | [phase-2.10b-mtls.md](phase-2.10b-mtls.md) | see git log | ✅ done |
| 2.10c — Metrics (Prometheus) | [phase-2.10c-raft-metrics.md](phase-2.10c-raft-metrics.md) | see git log | ✅ done |
| 2.10d — Partition harness | [phase-2.10d-partition-harness.md](phase-2.10d-partition-harness.md) | `multinode` | ✅ done |
| 2.11 — Boot dispatch + cluster data plane v1 | [phase-2.11-cluster-boot-dispatch.md](phase-2.11-cluster-boot-dispatch.md) | see git log | ✅ done |
| 3.1 — S3 object store (snapshot offload + WAL archival) | [phase-3.1-s3-object-store.md](phase-3.1-s3-object-store.md) | `multinode` | ✅ done |
| 3.2 — Rolling upgrades (zero-downtime version migration) | [phase-3.2-rolling-upgrades.md](phase-3.2-rolling-upgrades.md) | `multinode` | ✅ done |
| 3.3 — Cluster-aware Python SDK | [phase-3.3-cluster-sdk.md](phase-3.3-cluster-sdk.md) | `multinode` | ✅ done |
| 3.5 — Per-tenant API keys + RBAC | [phase-3.5-api-keys-rbac.md](phase-3.5-api-keys-rbac.md) | `multinode` | ✅ done |
| 3.6 — Crypto-shredding (GDPR erasure) | [phase-3.6-crypto-shredding.md](phase-3.6-crypto-shredding.md) | `multinode` | ✅ done |
| 3.7 — `valori import` (Qdrant + JSONL migration) | [phase-3.7-valori-import.md](phase-3.7-valori-import.md) | `multinode` | ✅ done |
| 3.4 — As-of / point-in-time reads | [phase-3.4-as-of-reads.md](phase-3.4-as-of-reads.md) | `multinode` | ✅ done |
| 5.1 — Verifiable AI: Proof-Carrying Answers + Compliance Pack | [phase-5.1-verifiable-ai.md](phase-5.1-verifiable-ai.md) | `multinode` | ✅ done |
| B13 — Snapshot cadence + startup readiness gate | [phase-B13-snapshot-readiness.md](phase-B13-snapshot-readiness.md) | `multinode` | ✅ done |
| C0 — Eval harness (recall@k, citation, provenance) | [phase-C0-eval-harness.md](phase-C0-eval-harness.md) | `multinode` | ✅ done |
| C0 plan — Cortex converged build plan | [phase-C0-cortex-plan.md](phase-C0-cortex-plan.md) | `multinode` | 📋 plan |
| C1 — Contextual retrieval + audited enrichment | [phase-C1-contextual-retrieval.md](phase-C1-contextual-retrieval.md) | `multinode` | ✅ done |
| C2 — Audited entity graph + provenance receipt | [phase-C2-entity-graph.md](phase-C2-entity-graph.md) | `multinode` | ✅ done |
| C3 — Self-maintaining memory (UI-only; **superseded by C4**) | [phase-C3-self-maintaining-memory.md](phase-C3-self-maintaining-memory.md) | `multinode` | ⚠️ superseded |
| C4.1 — Kernel-native time decay (self-maintaining pillar 1) | [phase-C4.1-decay.md](phase-C4.1-decay.md) | `multinode` | ✅ done |
| C4.1b — Cluster decay + state-machine creation timestamps | [phase-C4.1b-cluster-decay.md](phase-C4.1b-cluster-decay.md) | `main` | ✅ done |
| C4.2 — Memory consolidation (self-maintaining pillar 2) | [phase-C4.2-consolidation.md](phase-C4.2-consolidation.md) | `main` | ✅ done |
| C4.3 — Contradiction detection (self-maintaining pillar 3) | [phase-C4.3-contradiction.md](phase-C4.3-contradiction.md) | `main` | ✅ done |
| 3.8 — Write-throughput regression gates | [phase-3.8-write-regression.md](phase-3.8-write-regression.md) | `multinode` | ✅ done |
| 3.9 — Terraform modules (AWS + Azure) | [phase-3.9-terraform.md](phase-3.9-terraform.md) | `multinode` | ✅ done |
| 3.10 — Signed releases + SBOM | [phase-3.10-signed-releases.md](phase-3.10-signed-releases.md) | `multinode` | ✅ done |
| 3.11 — Concurrent reads via RwLock engine | [phase-3.11-rwlock-engine.md](phase-3.11-rwlock-engine.md) | `multinode` | ✅ done |
| 3.12 — Batch insert per-item idempotency | [phase-3.12-batch-idempotency.md](phase-3.12-batch-idempotency.md) | `multinode` | ✅ done |
| 3.13 — HNSW parameter exposure | [phase-3.13-hnsw-params.md](phase-3.13-hnsw-params.md) | `multinode` | ✅ done |
| 3.14 — MCP server (verifiable agent memory) | [phase-3.14-mcp-server.md](phase-3.14-mcp-server.md) | `multinode` | ✅ done |
| 3.15 — Native GraphRAG (one-call retrieval) | [phase-3.15-graphrag.md](phase-3.15-graphrag.md) | `multinode` | ✅ done |
| 6 — Persistent, isolated projects (UI workspace) | [phase-6-persistent-projects.md](phase-6-persistent-projects.md) | `main` | ✅ done |
| 6.1 — Replication factor in the project wizard: "Single Node" / "3-Node Cluster" cards; multi-node `ProjectEntry.nodes[]` manifest (auto-migrating), dedicated 4010-4999 cluster port range, aggregate "2/3 running" status; unifies the two divergent dimension lists and shares cluster-config helpers with `/launch` | [phase-6.1-project-wizard-replication.md](phase-6.1-project-wizard-replication.md) | `Node-scaleup` | ✅ done |
| 6.2 — Snapshot autosave + verified reopen: UI-spawned nodes set `VALORI_SNAPSHOT_INTERVAL=60` (periodic autosave survives ungraceful kills); cluster mode gains graceful shutdown (clean redb close); cluster `/search` score fixed from raw Q16.16 `i64` to standalone-matching `f32`; close route persists final record count. E2E-verified: standalone + 3-node cluster survive create→insert→close→reopen with data intact | [phase-6.2-snapshot-autosave.md](phase-6.2-snapshot-autosave.md) | `Node-scaleup` | ✅ done |
| C5 — Valori Reranker (hybrid retrieval) | [phase-C5-valori-reranker.md](phase-C5-valori-reranker.md) | `main` | ✅ done |
| I1 — Server-side document chunking (`/v1/ingest/document`) | [phase-I1-server-chunking.md](phase-I1-server-chunking.md) | `main` | ✅ done |
| I2 — On-node embedding + full pipeline (`/v1/ingest`) | [phase-I2-on-node-embedding.md](phase-I2-on-node-embedding.md) | `main` | ✅ done |
| I3 — UI wired through server pipeline with auto-fallback | [phase-I3-ui-server-pipeline.md](phase-I3-ui-server-pipeline.md) | `main` | ✅ done |
| I4 — `/v1/ingest` wired into cluster mode (Raft path) | [phase-I4-cluster-ingest.md](phase-I4-cluster-ingest.md) | `main` | ✅ done |
| I5 — Tree-RAG: hierarchical retrieval + citations + replayable receipts | [phase-I5-tree-rag.md](phase-I5-tree-rag.md) | `main` | ✅ done |
| I6 — Community layer: Label Propagation + centroid search + entity extraction | [phase-I6-community-layer.md](phase-I6-community-layer.md) | `main` | ✅ done |
| I7 — Metadata filtering: JSON predicate post-filter on `/search` (both paths) | [phase-I7-metadata-filter.md](phase-I7-metadata-filter.md) | `main` | ✅ done |
| P1 — Million-scale performance: growable-Vec snapshots (fixes `CapacityExceeded` at 1M), WAL flush-on-drop, SIMD L2, benchmark suite | [phase-P1-million-scale-performance.md](phase-P1-million-scale-performance.md) | `main` | ✅ done |
| P2 — IVF centroid auto-scaling (k = sqrt(N)); `needs_rebuild()` hook; `VALORI_IVF_N_LIST`/`VALORI_IVF_N_PROBE` overrides | [phase-P2-ivf-centroid-scaling.md](phase-P2-ivf-centroid-scaling.md) | `main` | ✅ done |
| S1 — Multi-Raft consensus skeleton: `ShardId` + shard-routed gRPC transport + per-shard redb/state-machine bootstrap loop; `VALORI_SHARD_COUNT` (default 1, symmetric placement, no namespace routing yet) | [phase-S1-multi-raft-skeleton.md](phase-S1-multi-raft-skeleton.md) | `Node-scaleup` | ✅ done |
| S2 — Raft-replicated namespace/collection creation: fixes a pre-existing cluster-mode bug (collection create was per-node, unreplicated); `KernelEvent::AutoCreateNamespace`/`DropNamespace`, `ClusterNamespaceRegistry` in the consensus state machine | [phase-S2-namespace-replication.md](phase-S2-namespace-replication.md) | `Node-scaleup` | ✅ done |
| S3 — Shard-routing: `shard_for_namespace()` + `DataPlaneState` multi-shard aware; S3a fixed a pre-existing bug (`Auto*` writes always landed in namespace 0, `ClientRequest.namespace_id` added, ~63 call sites fixed); S3b wired `cluster_memory_upsert`/`cluster_list_nodes`/`cluster_memory_search` to real namespace-correct, shard-routed data | [phase-S3-shard-routing-infrastructure.md](phase-S3-shard-routing-infrastructure.md) | `Node-scaleup` | ✅ done |
| S4 — Extends S3b's routing to `cluster_memory_consolidate`, `cluster_extract_entities` (also fixed a pre-existing id-allocation race), and `cluster_ingest` — every collection-aware write handler now routes to its namespace's shard | [phase-S4-remaining-write-handlers.md](phase-S4-remaining-write-handlers.md) | `Node-scaleup` | ✅ done |
| S5 — Crypto-shredding cross-shard safety: `cluster_insert_encrypted` routes by namespace; `DELETE /v1/crypto/shred/:key_id` fans out to every shard and aggregates per-shard status (`shredded: bool` true only when all shards confirm) | [phase-S5-crypto-shredding-cross-shard.md](phase-S5-crypto-shredding-cross-shard.md) | `Node-scaleup` | ✅ done |
| S6 — Shard-aware linearizable read-index: `ensure_read_consistency(shard_id, ...)` + `/v1/cluster/read-index?shard=N`; also gave `cluster_memory_search` a read-index check it never had | [phase-S6-shard-aware-read-index.md](phase-S6-shard-aware-read-index.md) | `Node-scaleup` | ✅ done |
| S7 — Core CRUD (`/v1/records`, `/v1/search`, `/v1/delete`, `/v1/soft-delete`, `/v1/vectors/batch-insert`) gains `collection` + shard routing; fixed a pre-existing SDK bug where `soft_delete()` hard-deleted (wrong endpoint) | [phase-S7-core-crud-routing.md](phase-S7-core-crud-routing.md) | `Node-scaleup` | ✅ done |
| S8 — Graph node/edge CRUD, `/v1/graphrag`, and `/v1/community/detect` (namespace-scoped) now route to their collection's shard instead of always shard 0; cross-shard community detection (no namespace filter) explicitly kept out of scope | [phase-S8-graph-community-routing.md](phase-S8-graph-community-routing.md) | `Node-scaleup` | ✅ done |
| S9 — Automated `cluster_ingest` coverage via an in-process mock embed server; fixed `cluster_tree_hybrid`'s vector-search section (resolved namespace but scanned shard 0 regardless) — the bug S1 originally flagged and never revisited | [phase-S9-ingest-coverage-tree-hybrid.md](phase-S9-ingest-coverage-tree-hybrid.md) | `Node-scaleup` | ✅ done |
| S10 — Fixed `valoricore-ffi`'s compile break: `get_timeline()`'s exhaustive `KernelEvent` match was missing `AutoCreateNamespace`/`DropNamespace` arms since S2; confirmed pre-existing on `main`, verified via a real `maturin build --release` | [phase-S10-ffi-namespace-events.md](phase-S10-ffi-namespace-events.md) | `Node-scaleup` | ✅ done |
| S11 — Python SDK: added `collection` param to `create_node`/`get_node`/`create_edge`/`get_edges`/`subgraph`/`neighbors` on both `SyncRemoteClient` and `AsyncRemoteClient` — these six methods had no collection support at all despite the server (standalone always, cluster since S8) fully supporting it | [phase-S11-sdk-graph-collection-param.md](phase-S11-sdk-graph-collection-param.md) | `Node-scaleup` | ✅ done |
| S12 — Fixed a standalone/cluster wire-format mismatch on `GET /v1/graph/node/:id` and `GET /v1/graph/edges/:id` (different field names per mode, e.g. `record` vs `record_id`) that made the Python SDK's `walk()`/`expand()`/`neighbors()` throw `KeyError` against cluster nodes; found while writing S11's docs | [phase-S12-graph-wire-compat.md](phase-S12-graph-wire-compat.md) | `Node-scaleup` | ✅ done |
| S13 — Fixed a real audit-log gap: `bootstrap_cluster()` only ever gave shard 0 a real audit sink, shards ≥ 1 silently got `NullAuditSink` (events discarded, no BLAKE3 chain) — a stale S1-era assumption invalidated by S3-S9's real shard-routed HTTP traffic. Every shard now gets its own genuine, chain-verifiable `events-shardN.log`; found while scoping the UI shard-count feature | [phase-S13-per-shard-audit-sinks.md](phase-S13-per-shard-audit-sinks.md) | `Node-scaleup` | ✅ done |
| S14 — First UI surface for sharding: "Shards" control (1/2/4/8) in the project-creation wizard, cluster projects only; `shardCount` persisted in the project manifest and threaded to `VALORI_SHARD_COUNT` on every spawned node. Verified live: 3-replica/2-shard project produced 6 independently chain-valid per-node-per-shard audit logs | [phase-S14-ui-shard-count.md](phase-S14-ui-shard-count.md) | `Node-scaleup` | ✅ done |
| S19 — Cross-shard timeline ordering: `shard_id` field added to `TimelineEntry`; composite sort key `(timestamp_unix, shard_id, log_index)`; validation pass rejects non-monotonic per-shard `log_index`; 1 new integration test | [phase-S19-cross-shard-ordering.md](phase-S19-cross-shard-ordering.md) | `Node-scaleup` | ✅ done |
| S18 — V4 event-log format with per-entry CRC32: `VERSION_V4`, `encode_header_v4`, `CRC32_SUFFIX_LEN=4`; `encode_entry(V4)` appends CRC32 of bincode bytes; `decode_entry(V4)` rejects on mismatch; new segments open as V4; chain hash unchanged (CRC is transport-only); 6 new hardening tests | [phase-S18-v4-per-entry-crc32.md](phase-S18-v4-per-entry-crc32.md) | `Node-scaleup` | ✅ done |
| S17 — CRTS/BCRP snapshot roundtrip tests: 5 new tests covering `created_at` timestamp survival, BM25 corpus survival, and forward-compat with pre-CRTS/pre-BCRP snapshots; added `Engine::reranker_corpus_len()`, `Engine::reranker_rerank()`, `ValoriReranker::corpus_len()` | [phase-S17-crts-bcrp-snapshot-tests.md](phase-S17-crts-bcrp-snapshot-tests.md) | `Node-scaleup` | ✅ done |
| S16 — Multi-shard audit surface: replaced `DataPlaneState.event_log_path` (shard 0 only) with `shard_event_log_paths: BTreeMap<ShardId, PathBuf>`; `/v1/proof/event-log` now returns per-shard BLAKE3 hashes; `/v1/timeline` merges all shards' logs sorted by wall-clock time | [phase-S16-multi-shard-audit-surface.md](phase-S16-multi-shard-audit-surface.md) | `Node-scaleup` | ✅ done |
| S15 — Fixed standalone collection loss on restart: the audit log recorded events without a namespace, so recovery replayed every event into the default collection and named collections came back empty. New append-only `LogEntry::EventNs` variant records the namespace; commit + replay + all readers (verify, timeline, inspect, replication) are namespace-aware. Verified live: upload→close→reopen now preserves the collection | [phase-S15-namespaced-event-log.md](phase-S15-namespaced-event-log.md) | `Node-scaleup` | ✅ done |
| A2 — `valori-storage`: durable storage layer extraction; WAL, event log, event journal, event commit, event replay, event proof, crash recovery, object store (S3/file) moved from `valori-node` into a new `no_std`-free crate; `valori-node` re-exports all modules so zero call-site imports change; `StorageError` + `From<StorageError> for EngineError` added | [phase-A2-valori-storage.md](phase-A2-valori-storage.md) | `Node-scaleup` | ✅ done |
| A1 — `valori-core`: zero-dependency type foundation; extracted `RecordId`, `NodeId`, `EdgeId`, `NamespaceId`, `CollectionId`, `ExecutionId`, `ShardId`, `ClusterEpoch`, `NodeKind`, `EdgeKind`, `Version`, `CoreError` into a new `no_std` crate; `valori-kernel` now re-exports from it | [phase-A1-valori-core.md](phase-A1-valori-core.md) | `Node-scaleup` | ✅ done |
| A12 — Effect bus wiring: `POST /v1/records` (standalone) now flows through `EffectBus → EngineKernelCapability → engine` via `run_graph_inline`; `capabilities.rs` updated to new `apply_command(body: &KernelCommandBody) → Result<serde_json::Value>` signature; `EffectError::Capacity` added for 507 propagation; `CapabilityRegistry` + `TaskRegistry` built at `build_router_with_keys` and injected as axum Extensions | [phase-A12-effect-bus-wiring.md](phase-A12-effect-bus-wiring.md) | `Node-scaleup` | ✅ done |
| A11 — Real `OperationHash` in receipt bridge: replaced timestamp-based placeholder with canonical RFC-0003 `BLAKE3(kind ‖ bincode(inputs) ‖ bincode(policy))`; added `OperationKind::Delete` + `BatchInsert` and matching `OperationInputs` variants; wired receipt emission into `batch_insert`, `delete_record`, `soft_delete_record` on both standalone and cluster paths | [phase-A11-real-op-hash.md](phase-A11-real-op-hash.md) | `Node-scaleup` | ✅ done |
| A10 — Receipt bridge: `receipt_bridge.rs` (`emit_write`/`emit_read`); wired into `insert_record` + `search` on both standalone and cluster paths; cluster `insert_record` switched from `raft_write` to `raft_write_data` to capture `resp.state_hash` + `resp.log_index`; `GET /v1/proof/receipt` now returns real receipts from live traffic | [phase-A10-receipt-bridge.md](phase-A10-receipt-bridge.md) | `Node-scaleup` | ✅ done |
| A9 — Node cleanup + `RaftKernelCapability`: real cluster `KernelCapability` wrapping `raft.client_write()` + `ValoriStateMachine::state_hash()`; `NoRaftKernelCapability` demoted to test-only stub; all tests green | [phase-A9-node-cleanup.md](phase-A9-node-cleanup.md) | `Node-scaleup` | ✅ done |
| A8 — `ReceiptAssembler` + `/v1/proof/receipt` endpoints: `Receipt`, `ReceiptHash`, `StateHash`, `ReceiptEnvelope`, `ReceiptAssembler`, `verify_receipt()`, `ReceiptStore`; `GET /v1/proof/receipt` + `GET /v1/proof/receipt/:id` on both standalone and cluster routers; 16 `valori-effect` tests | [phase-A8-receipt-assembler.md](phase-A8-receipt-assembler.md) | `Node-scaleup` | ✅ done |
| A7 — TaskRunner + real capabilities in `valori-node`: `EngineKernelCapability` (wraps `SharedEngine` → `apply_committed_event_ns` + BLAKE3 hash), `HttpEmbedCapability` (wraps `embed_batch()`), `PassthroughHttpCapability`, `CapabilityRegistryBuilder`; `TaskRegistry` (all 12 `TaskKind`s wired), `TaskRunner` (topological walk, predecessor threading, retry, handle lifecycle), `run_graph()` convenience fn; 3 unit tests | [phase-A7-task-runner.md](phase-A7-task-runner.md) | `Node-scaleup` | ✅ done |
| A6 — `valori-effect`: Effect system + EffectBus; `EffectId` (`BLAKE3(exec_id ‖ task_idx ‖ effect_idx)`, stable across retries), `EffectDurability` (Durable/Ephemeral), `EffectPayload` (KernelWrite/Receipt/Audit/Counter/Gauge), `EffectBus` (dedup `Mutex<HashSet>`, dispatch + `dispatch_all`); 7 capability traits: `KernelCapability`, `EmbedCapability`, `LlmCapability`, `StorageCapability`, `HttpCapability`, `ProofCapability`, `SchedulerCapability`; `CapabilityRegistry`; `Task` trait, `TaskContext`, `TaskOutput`; 3 concrete tasks: `EmbedTask`, `InsertRecordTask`, `SearchTask`; `NoOpKernelCapability`; 9 tests | [phase-A6-valori-effect.md](phase-A6-valori-effect.md) | `Node-scaleup` | ✅ done |
| A5 — `valori-planner`: Operation lifecycle and execution planning; `Operation` (content-addressed with `OperationHash`), `OperationInputs` (14 kinds, planning params only — no vectors/text), `PlannerFingerprint`, `PlanningContext`, `PlanningContextHash`, `ExecutionGraph` (DAG with Kahn topo sort + `GraphHash`), `ExecutionCache` (in-process `RwLock<HashMap>`), `ExecutionHandle` (tokio watch), `ExecutionRegistry`, `NoOpPlanner`, `IngestPlanner`, `plan_with_cache()` (2-layer: in-process → durable DB); 16 tests | [phase-A5-valori-planner.md](phase-A5-valori-planner.md) | `Node-scaleup` | ✅ done |
| A4 — `valori-metadata`: control-plane persistence crate; `Project`, `IndexKind`, `CollectionRegistry` (elevated `NamespaceRegistry`), `ShardTopology`, `SnapshotCatalog`, `ExecutionRecord`, `ExecutionRetentionPolicy`, `PlannerCacheKey/Entry`; `MetadataDb` (redb v2, 5 typed tables); 13 tests | [phase-A4-valori-metadata.md](phase-A4-valori-metadata.md) | `Node-scaleup` | ✅ done |
| A3 — `valori-state`: state lifecycle crate; `bootstrap.rs` (moves `recovery.rs` out of `valori-storage`), `manifest.rs` (`StateManifest`), `lifecycle.rs` (`StateLifecycle`), `shutdown.rs` (`shutdown_snapshot`), `StateError`; `valori-node` re-exports `valori_state::bootstrap as recovery` so zero call-site changes | [phase-A3-valori-state.md](phase-A3-valori-state.md) | `Node-scaleup` | ✅ done |
| RFC-0 — Specification: `rfcs/0000-glossary.md` (16 canonical terms, each with Definition/Owner/Lifetime/Invariant), `INVARIANTS.md` (15 system invariants), `COMPATIBILITY.md` (version policy for KernelABI, snapshot format, event log, wire types, HTTP API, rolling upgrade); `rfcs/0001-operation-lifecycle.md`, `rfcs/0002-kernel-contract.md`, `rfcs/0003-receipt-spec.md`, `rfcs/0004-capability-model.md`, `rfcs/0005-crate-boundaries.md` | *(this commit)* | `Node-scaleup` | ✅ done |
| I8 — Document update with chunk-level BLAKE3 content diffing: `POST /v1/ingest/update` accepts a `document_node_id` + new text, diffs old vs new chunks by content hash, keeps unchanged chunks (no re-embed), soft-deletes removed chunks, embeds+inserts only new/changed chunks. Both standalone and cluster (shard-routed) paths. Python SDK updated. | [phase-I8-document-update.md](phase-I8-document-update.md) | `Node-scaleup` | ✅ done |
| A13 — Planner migration: 8 standalone handlers (Snapshot, GraphRAG, MemorySearch, CommunityDetect, CommunitySearch, TreeBuild, TreeQuery, TreeHybrid) routed through `run_graph_inline`; 8 new `KernelCapability` default methods + `EngineKernelCapability` overrides; 5 new Task files; 6 new `TaskKind` variants; `Deserialize` added to `HybridHit`, `HybridResponse`, community/api types; 0 test failures | [phase-A13-planner-migration.md](phase-A13-planner-migration.md) | `Node-scaleup` | ✅ done |
| A14 — Node audit bug fixes: P0: state_hash zeros (block_in_place real BLAKE3), snapshot shard-0-only (loop all shards + correct field name), /health behind auth (restructured middleware), P1: namespace truncation (u16→u32 before modulo), community_search shard routing (resolve ns→shard), community_detect error swallowed (.ok() → ?), decay sort inverted (score*factor → score/factor); 0 test failures | [phase-A14-node-audit-bugfixes.md](phase-A14-node-audit-bugfixes.md) | `Node-scaleup` | ✅ done |
| A13.1 — Cluster planner wiring: `RaftKernelCapability` extended with 8 new methods (save_snapshot, graph_rag, memory_search, community_detect, community_search, tree_build, tree_query, tree_hybrid); 7 cluster handlers (graphrag, snapshot_save, tree_build, tree_query, tree_hybrid, community_detect, community_search) wired through `run_graph_inline`; cluster path now mirrors standalone path end-to-end; 0 test failures | [phase-A13.1-cluster-planner-wiring.md](phase-A13.1-cluster-planner-wiring.md) | `Node-scaleup` | ✅ done |

## Report template

Every report answers five questions:

1. **Goal** — what this phase was supposed to achieve (1–2 sentences)
2. **Delivered** — what actually landed, file by file where it matters
3. **Findings** — bugs and design gaps discovered during the work
   (often the most valuable section)
4. **Validation** — the evidence: test counts, demos, end-to-end runs
5. **Follow-ups** — anything consciously deferred, and to which phase
